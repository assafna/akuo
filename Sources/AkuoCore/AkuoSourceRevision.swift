public enum AkuoSourceRevision {
    // Packaging replaces this development placeholder only inside the immutable
    // archived source snapshot used for compilation.
    public static let current = "development"
}
